# 16:30 全国交易气象变化复核

复核 `{{LOCATION}}` 从当前至明天上午的交易相关气象信号，并与今天 09:00 全国晨报比较。`{{LOCATION}}` 是报告范围，不是单一气象查询地点。

1. 先使用 `read` 读取 `{{MORNING_BASELINE_FILE}}`。只有文件中的绝对日期和范围都与今天、`{{LOCATION}}` 一致时才作为基线；不要用长期记忆或猜测补出晨报。
2. 优先复查晨报中的重点省份、关键时间窗和高不确定区域，同时用官方全国天气与预警信息检查是否出现新的高影响区域。
3. 以下情况属于实质变化：省份进入或退出重点清单；官方预警新增、升级、降级或解除；主要天气过程、影响时段或强度档位明显改变；负荷、光伏、风电或净负荷的方向判断改变；关键时段移动约 2 小时以上；交易关注点或安全建议改变。
4. 单一来源的小幅数值波动、更新时间变化、不影响电力判断的尾差，或者仅换一种措辞，不属于实质变化。
5. 如果没有实质变化，只输出完全一致的 `NO_REPLY`，不要加标点、解释或 Markdown。
6. 如果缺少当天有效基线，不做伪比较；发送一条简短复核并明确写出“缺少全国晨间基线”。
7. 有变化时只列发生变化的省份、时间窗、天气信号和电力影响，不重复整份晨报，不推算电价或 MW。标题使用 `🔄 **全国交易气象变化提醒（M月D日 16:30）**`，并遵循附后的电力交易气象版式。
8. 不输出工具过程，不生成图片，不主动展示来源或算法说明。
